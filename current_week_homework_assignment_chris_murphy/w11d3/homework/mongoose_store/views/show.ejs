<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("partials/head") %>
</head>
<body>
    <h1>Products</h1>
    <h5><a href="/products">Back to the Products Page</a></h5>
    <div class="container">
       
        <div class="product-card">
            <div class="info">
                <h3><%= product.name %></h3>
                <h5><a href="/products/<%=id%>/edit">Edit this product</a></h5>
                <img src=<%= product.img %> alt=<%= product.name%>>
                <p><%= product.description%></p>
                <h4>Price: $<%= product.price %></h4>
                <% if(product.qty === 0) { %>
                    <h3 class="out-of-stock">OUT OF STOCK</h3>
                <% } else if (product.qty > 0 ) { %>
                    <h3><%= product.qty %> remaining</h3>
                    <form action="/products/<%= product.id%>/buy?_method=PUT" method="POST">
                        <input type="submit" value="BUY">
                    </form>
               <% } %> 
               <form action="/products/<%= product.id %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE">
            </form>
            </div>
        </div>
    </div>
</body>
</html>